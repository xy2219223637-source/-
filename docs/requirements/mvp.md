# MVP 范围

## 用途

定义最小但可信的产品范围。

## 填写内容

- 必做功能：
- 延后功能：
- MVP 内允许的人工操作：
- MVP 内允许的模拟或仿真集成：
- MVP 完成的退出标准：

## 规则

本文件只负责当前 MVP 边界。

不要在这里重复 `docs/architecture/project-vision.md` 里的长期产品愿景，也不要重复 `docs/design/app-overview.md` 里的当前应用行为。
