# 模块边界

## 用途

定义 `<project-name>` 的主要代码归属边界。

## 推荐章节

- 应用壳
- 领域模块
- 共享 UI/组件
- 数据/服务层
- 状态层
- 测试归属

## 填写内容

对于每个模块或包族，请记录：

- 职责
- 允许的依赖
- 禁止的依赖
- 受哪些 owner 文档约束

## 规则

如果某个反复出现的设计争论依赖模块归属，请把答案写在这里，不要在聊天里反复争论。
